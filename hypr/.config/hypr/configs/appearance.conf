#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/
windowrule = no_blur on, match:title ^()$, match:class ^()$

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(fab387ee)
    col.inactive_border = rgba(89b4faaa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 1

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
	blur {
        enabled = true
        size = 8
        passes = 4
        new_optimizations = true
        ignore_opacity = true
        xray = true
        noise = 0.0117
        contrast = 0.8916
        brightness = 0.8172
        vibrancy = 0.1696
        vibrancy_darkness = 0.0
        popups = true
        popups_ignorealpha = 0.5
    }

	windowrule = opacity 0.9 0.9, match:class ^(kitty|com.mitchellh.ghostty|spotify|zen|brave-browser|org.mozilla.Thunderbird|brave-.*-Default|org.qbittorrent.qBittorrent)$
    layerrule = blur on, match:class ^(popups|wofi|rofi|waybar|swaync-control-center|swaync-notification-window|logout_dialog|org.mozilla.Thunderbird|brave-.*-Default|org.qbittorrent.qBittorrent)$
    layerrule = ignore_alpha 0.5, match:class ^(rofi|wofi|waybar|swaync-control-center|swaync-notification-window|logout_dialog|org.mozilla.Thunderbird|brave-.*-Default|org.qbittorrent.qBittorrent)$

	windowrule = opacity 0.9 0.9, match:title ^(.*wants to save.*|Frame Checker|.*Find Directory.*|.*Open.*|.*Save.*|.*Write.*)$
    layerrule = blur on, match:title ^(.*wants to save.*|Frame Checker|.*Find Directory.*|.*Open.*|.*Save.*|.*Write.*)$
    layerrule = ignore_alpha 0.5, match:title ^(.*wants to save.*|Frame Checker|.*Find Directory.*|.*Open.*|.*Save.*|.*Write.*)$

	windowrule = center on, match:title ^(.*wants to save.*|.*Find Directory.*|.*Open.*|.*Save.*)$
	windowrule = center on, match:class ^(.*nngceckbapebfimnlniiiahkandclblb.*|Frame Checker)$
	windowrule = float on, match:class ^(cava|Frame Checker)$
	windowrule = float on, match:title ^Bitwarden$
	windowrule = float on, match:title ^.*Write.*$
	windowrule = no_blur on, match:class ^cava$
	windowrule = no_shadow on, match:class ^cava$

	windowrule = opacity 0.9 0.9, match:class ^.*nngceckbapebfimnlniiiahkandclblb.*$
	windowrule = float on, match:class ^.*nngceckbapebfimnlniiiahkandclblb.*$
	layerrule = blur on, match:class ^.*nngceckbapebfimnlniiiahkandclblb.*$

}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

xwayland {
  force_zero_scaling = true
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
	session_lock_xray = true # necessary for transparency in hyprlock/swaylock
}

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppress_event maximize, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus on, match:class ^$, match:title ^$, match:xwayland 1, match:float 1, match:fullscreen 0, match:pin 0

